# Halcyon / eRhino

个人兴趣项目，目标是成熟、可长期演进的 RV64 微内核系统，以生产系统为主要参照。Halcyon 包含 eRhino 微内核、用户态系统服务及跨组件契约；用户态基础运行库为 `rinlib`。内核 Rust 组件的目标身份是 `kernel`，当前旧名的迁移由[内核命名计划](plans/todo-2026-09-22-kernel-identity.md)拥有。

## 心智模型

- **协作式微内核**：本项目采用内核态不可抢占的执行模型，每次内核推进必须有结构性工作上界。业务政策与可由用户态承担的长工作位于用户态；已提交且必须由特权级完成的维护责任由内核稳定持有、按预算分批推进，活性不依赖调用者继续调用。出现需要内核抢占或内核线程的需求时先检查工作归属。完整契约见 [kernel](notes/ideas/kernel.md)。
- **机制、授权与政策分离**：内核强制隔离、对象与 capability 等机制；用户态组合服务和资源政策。身份、付款与授权分别建模，资源和操作各有明确 owner；共享状态、事务、唤醒、取消与退休须形成完整责任链。
- **结构正确优先，审阅中简化（less is more）**：成熟系统的长期结构正确是首要目标，设计先理清领域、所有权、生命周期与演进边界，允许提前为未来能力铺路。简化主要依据成形实现与责任链的审阅，消除通往正确结构过程中的冗余、机制缠绕和不必要的间接层；以理解和修改成本判断收益，不在设计期用最小机制、最小步进、代码量或抽象数量限制方案空间。发现结构错误及时整体纠正，简化不得损害长期结构。
- **共享 ABI 同步演进**：`shared/erhino_shared` 拥有共享 ABI，变更公共数据结构、消息格式或调用号时同步内核、rinlib 及其他实际消费者，不留单边。`shared/` 中纯逻辑库的内部变更按实际依赖判断影响；ABI 不冻结。
- **领域与单一真值**：概念、类型、状态和可复用机制各有唯一拥有者，依赖从知识使用者指向提供者；不能用回调、重导出或 facade 隐藏反向依赖。用户态库分层由 [libraries/README](user/libraries/README.md) 拥有。

### 文档与事实归属

| 位置 | 拥有的内容 |
|---|---|
| `notes/ideas/` | 系统应该是什么：自顶向下的概念、边界、契约与长期设计理由，可脱离实现独立成立；不下沉到结构字段或代码引用 |
| `notes/impls/` | 当前实际怎么做：模块、数据与控制流、所有权、生命周期、并发、失败及未完成能力的行为限制，引用源码供核实；正文无需任务背景即可理解 |
| `plans/` | 任务范围、依赖、设计选择过程、施工进度、验证证据、阻塞与接力顺序；工作记录不重复定义通用流程 |

ideas 与 impls 是并列视角。判断方向读 ideas，核实当前行为读代码和 impls，判断本次承诺读唯一工作记录。发现不一致，按目标、外部证据和实际机制判断应修设计还是实现，不预判任何一方正确；计划不能覆盖正确性契约。

每个机制在 idea、impl 视角各有唯一拥有篇，其他篇链接使用，具体规则与主题索引见 [notes/README](notes/README.md)。施工期间 impls 也描述当前面貌，更新机制所在章节并清除过时叙述；运行结果和完成宣告留在工作记录。notes 根目录只放导读、索引与跨专题通用内容。

当前会话的明确决定立即生效。影响长期设计或后续接力的决定须在结束前写入所属文档；后续会话按记录接手。当前导航不得保留已失效的“下一步”，历史过程放入档案并链接追溯。

## 仓库与按需阅读

三个独立 Cargo workspace 以 path 依赖连接，分别组织内核运行时、跨层纯逻辑和用户态运行时；具体 target 与工具链以各 workspace 配置及构建手册为准。

| 路径 | 职责 |
|---|---|
| `os/kernel/` | eRhino 内核，`no_std`；Rust package、默认 target 与 crate identifier 统一为 `kernel` |
| `os/` 其余 crate | 内核侧可独立测试的机制库 |
| `shared/erhino_shared/` | 内核与用户态共享 ABI |
| `shared/` 其余 crate | 跨层可移植纯逻辑库 |
| `user/rinlib/` | 用户态基础运行环境与内核契约的安全封装 |
| `user/libraries/` | 按领域组织的公共库 |
| `user/services/`、`drivers/`、`tests/` | 系统服务、用户态驱动、验收进程 |
| `notes/`、`plans/` | 设计/实现说明、任务与过程档案 |

按当前任务触发阅读，不要求每次通读所有专题：

| 触发 | 入口 |
|---|---|
| 开始或恢复实施任务 | [COMPASS](plans/COMPASS.md) → 本任务唯一工作记录 → 受影响机制的 ideas / impls |
| 新增或改名 Rust 组件 | [用户态共同规则](user/README.md) 的命名章节（全仓 Rust 组件适用） |
| 新增用户态库、调整公共类型/依赖、组合跨领域能力 | 先读 [user/README](user/README.md)，再读 [libraries/README](user/libraries/README.md) |
| 构建、测试、选择 QEMU 路线或调整验收工具 | [构建与验证手册](plans/BUILD-AND-TEST.md) |
| 挂起、损坏、非确定性故障调查 | [DEBUG-PLAYBOOK](plans/DEBUG-PLAYBOOK.md)，涉及工具与平台时再读 [TOOLING-PITFALLS](plans/TOOLING-PITFALLS.md) |
| 硬件、ABI、协议的设计、编码或审查 | [外部契约索引](references/CONTRACTS.md)，定位固定版本及具体章节 |
| 需要外部系统对照 | [系统参考索引](references/systems/INDEX.md)，从需求广泛取样并核实官方资料 |
| 交付前结构检查或提交后独立审查 | [REVIEW](plans/REVIEW.md) |

## 操作与编码约定

- **授权**：只读调查可直接进行；实施前说明思路、范围与关键取舍并取得确认。已获授权范围内，内部结构、容量布局和实现选择自行决定，进入下一阶段不重复请求授权；新增范围、风险或改变已确认外部语义、关键前提时再确认。notes/plans 与目标冲突且无法自行闭合的架构选择交用户裁决。
- **提交与外部写入**：提交前展示 diff 或摘要并取得明确授权，只包含本次改动。commit 不授权 push、发布或其他外部写操作；它们分别取得许可。项目外写入和系统级安装遵守全局授权边界。
- **构建**：统一走 `just`，不裸跑 `cargo build`。host 单测必须显式指定 host target；具体命令、路线和平台边界见构建手册。长命令保存完整日志，终端只展示摘要；失败保留日志、路径与退出码，禁止用 `tail` 掩盖错误。
- **组件身份**：全仓 Rust 组件的目录叶名、Cargo package、默认 target 与 crate identifier 必须一致；不保留补偿名差的 alias。命名细则及改名同步面由 `user/README.md` 拥有。现有违规项登记整改，不据此缩小规则范围。
- **语言**：文档、注释、提交信息用中文；提交使用 Conventional Commits。内核/用户态日志、panic/assert/expect 消息和构建工具输出用正式英文。
- **安全与并发**：用户可触发的 fault 终止进程，不 panic 内核；未知 syscall 返回错误。`unsafe` 须有成立的 SAFETY 论证，`unwrap/expect` 仅用于有论证的结构性不可达分支。状态按 hart 私有、对象、真正全局事实分层，禁用 `static mut`；先考虑单一所有权或消息传递，确需共享时压短临界区并写明单向锁序。公平性与唤醒活性须有结构性保证。
- **用户内存访问**：遵守 [mm 实现](notes/impls/mm.md) 的 uaccess 契约，使用 translate 前置校验与受保护的 SUM 直访，不另建软件遍历页表的拷贝路径。
- **提交署名**：`pre-ai`（975c46f）之前为手写实现。AI 辅助提交使用当前会话实际模型显示名与 provider 对应 noreply 邮箱生成 `Co-Authored-By: <模型显示名> <邮箱>`；无对应邮箱时用 `noreply@pi.dev`，不得伪造 provider 域名。不能确定实际模型时问用户。

## 任务适用范围与完成条件

只读调查不启动施工流程。实施若涉及公开契约、权限、所有权、生命周期、并发或跨组件责任链，执行下节完整流程；其他改动按影响范围实施、验证并更新已有记录，不为填满流程制造专题、设计图或测试。适用性按语义影响判断，不按文件数或代码行数判断；不适用的审计项简述理由。

**以下完成条件用于判断本次约定的任务是否可以交付，不用于要求每个编辑步骤或进度条目独立成立。** 专题计划补充具体契约与验证门：

1. 当前承诺的能力、非目标及边界明确。
2. 必需前置与当前承诺范围内的消费者已经接通。
3. 正常、失败、取消、退出、超时和释放/退款路径各有明确 owner，已提交责任不会因调用者消散而丢失；不适用项有理由。
4. 当前范围内应替换的旧路径、重复真值和过渡结构已删除。
5. 相应验证通过，交付前结构检查完成，ideas / impls 与工作记录各自准确。
6. 延期项不破坏上述契约，且有唯一记录、触发条件和完成标准。

基础机制可以按自身契约完成，记录用途、未来消费者及接通条件；系统能力完成则要求其承诺的真实消费者接入。源码存在、局部检查通过或施工阶段结束均不能替代对应完成条件。铺路或待接通能力须明确标注，不冒充系统能力交付。

## 标准施工流程

专题计划记录目标、依赖、设计选择和专属完成门；已有计划接力不另开平行方案。步骤中发现前提不成立，回到相应步骤更新唯一工作记录后继续。

1. **接手与基线**：核对分支、提交、工作树、COMPASS、唯一工作记录、目标/非目标和前置证据；保留其他任务未提交改动。
2. **责任链审计**：盘点实际消费者、ABI 两侧、owner/authority、资源来源、锁序、跨 hart、失败与收束路径、旧机制和外部契约，列出未知项与缺失前置。
3. **任务与依赖**：依据目标结构和共同变化的契约确定实施范围，将同一切面涉及的类型、接口、状态转换、消费者及清理路径统一规划迁移。范围内按依赖安排编辑顺序，中间步骤不要求独立交付或通过整体验收。只有存在明确的独立契约或分阶段交付需求时才拆分阶段，不以函数、文件或断言作为默认阶段边界。记录前置、失败边界、旧路径删除条件、验证门及自然顺序，缺失的独立前置另行立案。
4. **设计闭合**：确定相关类型、所有权/授权、状态机、线性化点、锁阶、停驻/唤醒与失败收束。方向结论进入 ideas，待实施方案进入工作记录；未决选择必须解决或明确阻塞依赖它的施工。
5. **按依赖实施**：自底向上接通公共契约、核心机制、消费者与清理路径，删除被替换的旧路径；中途保留的债务遵守下节登记规则。
6. **分层验证**：开发期间使用编译、host/target 检查、针对性 testcase，也可复用现有 QEMU 路线定位集成错误，均标记为开发证据。开发检查用于发现目标实现中的问题；对于已规划但尚未完成的迁移，继续按目标结构实施，不为使中间状态通过检查而补建临时机制。代码责任链接通后执行 `just check`、`just clippy` 和必要的 QEMU/平台/退出组合；路线选择与聚合验收见构建手册。开发检查复用正式消费者与既有测试设施，不为未闭合机制另造测试专用服务、临时消费者或测试专用运行体。
7. **交付前结构检查**：按 REVIEW 手册核对最终结构与已有 findings，包括 bug、性能、锁序、owner、旧路径、重复真值和文档；检查结果写回唯一工作记录。
8. **组合收口与归档**：完成跨机制验证，逐项核对上节完成条件，更新工作记录与 COMPASS，完成记录归档。提交须单独授权；提交后登记固定提交的独立 Review。独立 Review 不替代本次交付前检查与验证，也不要求在本次交付前完成。

验证以降低具体风险为目标，优先复用现有设施。新增测试须说明要捕获的错误及独立契约依据，不照抄实现作为预期；先在可观察行为的最小边界验证，风险跨组件时才扩大。验证环境隔离，临时数据与进程结束后清理；证据须区分开发检查、完整验收和未覆盖部分。检查通过后不重复运行，除非新改动、新失败或具体未解决风险需要补测。

### 跨会话接力

未完成时，把已实施部分、未闭合责任、当前代码/文档事实、阻塞、下一步及验证状态写回唯一工作记录，并在 COMPASS 保留简短入口。同步更新受影响的 impls；方向或契约变化同步 ideas。后续从该断点继续，不重复已完成工作。

专题实施的唯一工作记录通常为 `todo-*.md`；独立 Review 发现的问题由其活跃 `review-*.md` 统一承载修复与复核，不另建重复 todo。未满足完成条件时不归档，也不登记为完成；允许记录阶段进度与开发证据。

## 设计取舍与债务

- **独立理由**：从需求推导，以成熟生产系统为主要参照；兼容、延续现有代码或 `pre-ai` 均不能单独构成理由。引用外部实现先查证官方资料，外部契约按固定规范取证，记忆不作证据。
- **范围与长期结构**：为使当前承诺成立而必需的重构属于本次闭包；只改善邻接机制且不影响当前契约的重构另行记录。性能问题先在既定范式内优化，范式被证据推翻时再修改；迁移成本与 ABI 破坏不是保留已知错误结构的理由。
- **已有铺路与施工中间态**：尚未完成的接线、临时布局和验证缺口不自动证明方向错误。未来能力可依据系统目标、可预见的变化与独立契约提前建设，不要求先有即时消费者；已有实现也不构成拒绝更好结构的理由。原型期发现边界或机制错误，应在相关责任链内整体替换并删除旧路径，不接受为了维持错误结构而叠加补丁、adapter 或特殊分支。过渡实现记录目标、替换触发与删除条件，随设计认识和消费者完善逐步替换；没有即时消费者或当前只有一个实现，均不足以单独否定抽象。
- **工程限额**：栈、guard、预算及审计阈值须来自硬件、ABI、正确性或资源成本。缺少依据的旧限额应重新评估，按资源模型决定保留、调整或取消，并同步布局、审计及文档；可推导阈值只保留一个真值来源。
- **债务与延期的唯一记录**：暂留的旧结构、重复表示、adapter、未决抽象、验证缺口和范围外残留，统一记入所属工作记录；写明现状/位置、目标/替代物、owner、触发条件、保留期限或接通条件、删除条件、验证标准与顺序。未登记债务不得进入下一阶段。当前闭包内可消灭的残留直接清理；延期只允许缩小后仍语义闭合且补齐收益较低的能力，不允许留下当前承诺的正确性欠账。完成后关闭条目，长期知识进入 notes。

## plans 命名与生命周期

| 名称 | 职责与生命周期 |
|---|---|
| 全大写，如 `COMPASS.md`、`REVIEW.md` | 常驻导航或专题手册；各自拥有唯一规则，不累积任务过程 |
| `todo-<日期>-<主题>.md` | 专题计划与工作记录，完成后归档；同一任务只有一个当前入口 |
| `todo-<日期>-<主题>-review.md` | 提交后生成的独立审查计划，记录固定提交与审查范围，执行纪律见 REVIEW 手册 |
| `review-<日期>-<主题>.md` | 调查/审查报告；存在 findings 时可更新处置与复核，保留原始发现及证据，关闭后归档 |
| `ref-<日期>-<主题>.md` | 只读参考资料或取证快照 |
| `archived/` | 已结束的工作记录与历史证据，只进不出；内容保持当时事实，新任务在活跃记录中引用承接 |

新类型前缀须能一句话说明生命周期，不为同一问题复制工作记录。COMPASS 只导航当前任务、触发条件和证据入口，不另存一套进度账本。

## 已知问题

@KNOWN_ISSUES.md
