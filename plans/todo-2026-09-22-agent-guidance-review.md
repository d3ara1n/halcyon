# Agent 规范与导航固定提交审查

## 对象与状态

待独立 Review。目标提交 `60e3253`（`docs: 整理代理规则、完成条件与接力导航`），父提交 `2811d48`。本计划在目标提交之后登记，只安排文档审查，不构成新的代码验收或 FAL 施工任务。

实施与验证证据见[整理档案](archived/todo-2026-09-22-agent-guidance.md)，整理前导航见[只读快照](archived/ref-2026-09-22-compass-snapshot.md)。审查方法遵守 [REVIEW](REVIEW.md)。本登记及 COMPASS 中的对应链接属于该提交的审查元数据，不递归生成另一份审查计划。

## 改动概要

- AGENTS 收敛任务适用范围、唯一完成条件、授权与文档效力、债务和接力规则。
- REVIEW 区分交付前结构检查与提交后独立审查，明确 findings 的唯一工作记录及报告生命周期。
- BUILD-AND-TEST 集中构建、QEMU、平台与证据边界，修正文档中的工具链、target、check 和 acceptance 事实。
- COMPASS 缩为当前任务和证据索引，原历史叙述及未提交 FAL 导航更新保存在快照中。
- kernel idea 明确有界推进和内核提交后责任；全仓组件身份规则不变，`kernel` 改名仅登记后续计划。

## 审查重点

1. 原有架构、安全、授权和语义闭包要求是否保留，规则去重是否引入隐含豁免或新的互相冲突。
2. 只读/局部工作与完整施工流程的触发是否明确，基础机制完成与系统能力交付是否区分，开发检查是否仍不能冒充验收。
3. 交付前检查、独立 Review、findings 修复及归档是否各有唯一责任记录，无重复计划或流程死循环。
4. kernel idea 是否与对象收束契约一致，内核维护的活性是否仍独立于用户后续调用；本次没有修改内核执行模型。
5. COMPASS 是否保留 FAL 断点、阻断项、原活跃计划与证据导航；快照能否还原整理前记录，历史“下一步”是否退出当前导航。
6. 构建手册是否符合目标提交的实际配置，当前 `erhino_kernel` 与未来 `kernel` 是否清楚区分，命名规则是否继续全仓生效。
7. 文档链接、有效命令、过程与事实归属是否一致；不批量改写历史基线，也不将本次文档检查当作 FAL 实现验收。

## 证据与边界

目标提交交付前已完成本地链接/锚点检查、配置对照、快照还原和差异检查；详见整理档案。本次未运行 Rust/QEMU，未改 Cargo、Justfile 或实现代码。该提交不证明仍在工作树中的 FAL 改动正确，也未实施内核改名。

独立审查形成报告后，由报告承载 findings 的修复与复核，本计划记录移交入口后归档。
