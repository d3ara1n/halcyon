# 重写版系统审查

本审查面向当前整棵代码树，不以 diff 或旧实现为边界。目标是证明外部契约、内部设计和实现相互一致，并显式暴露尚无法证明的假设。

## 证据

外部契约的证据层级与使用方式见 `references/README.md`。每项外部语义结论必须引用固定规范的路径与小节；项目内部语义必须引用 `notes/`。`pre-ai` 及旧 review 只用于构造反例和回归问题。

规范没有给出唯一答案时，finding 必须标为设计决策，不得把实现习惯提升为契约。实现参照与规范冲突时，以规范和项目已经确认的设计为准。

## 分片

按依赖顺序审查，每片固定自己的起始 commit：

1. SBI 边界与地址纪律（已完成，[分片报告](review-2026-08-audit-01-sbi.md)）；
2. trapframe、上下文切换与 CSR（已完成，[分片报告](review-2026-08-audit-02-trap-context.md)）；
3. 启动、页表与 TLB；
4. SMP、调度与对象生命周期；
5. syscall 与 `shared/` ABI；
6. IPC、FAL 与用户态服务；
7. 工程化收口。

后片可以使用前片已经确认的结论；前片未决时，后片不得把它当作前提。

## 单片流程

### 1. 固定基线

记录 review 开始时的 commit、目标文件和规范版本。完成标准：影响该契约的入口、出口、调用者和共享结构全部进入代码表面。

### 2. 提取契约

从固定规范和 `notes/` 建立契约表，每条包含原文所在文件与小节。完成标准：review 中使用的每个常量、布局、状态转换和地址语义都有来源，或明确标为待决策。

### 3. 映射实现

沿调用关系和状态所有权映射实现，不按目录浏览代替控制流追踪。完成标准：正常路径、错误路径、初始化路径、跨 hart 路径和退出路径均已覆盖。

### 4. 对照审查

逐条尝试证伪契约与实现的一致性。检查重点是遗漏状态、部分保存、错误参数、地址域混淆、溢出、并发可见性和失败语义，而非代码风格。

### 5. 独立核验

由未参与首次审查的 reviewer 核对：

- 规范引用是否支持 finding；
- 代码路径是否真实可达；
- 是否遗漏相邻调用点；
- 建议是否偷偷引入未经确认的架构选择。

没有通过核验的条目保留为“待证明”，不能写成确定 bug。

### 6. 收口

报告使用附录的 finding 模板。架构级问题先给选项并由用户拍板；结论可推出的宽泛设计写入 `notes/`。代码修复与报告分开进行，修复后再以该 finding 为 spec 做 diff review。

## 分类

- **缺陷**：存在明确契约违反和可达路径。
- **未证明不变量**：实现依赖某假设，但代码、规范或同步链不足以证明。
- **设计缺口**：外部规范允许多种语义，而项目尚未决定。
- **文档漂移**：`notes/`、注释或 ABI 描述与实现不一致。
- **观察项**：证据不足，保留调查方向但不驱动修改。

严重度与置信度分别记录，不能用高严重度掩盖低置信度。

## 审查纪律

- 一份报告只覆盖一个分片，不把顺手发现的其它层问题展开；跨片问题登记到对应分片。
- 报告呈现当前事实，不记录猜测性的修复历史。
- review 阶段不边看边改；主审先形成自洽报告，再做一次独立核验。reviewer 不充当交互式共同设计器，不以“改一点—审一点”的循环替代主审推导；下一次核验留给完整修复 diff。
- 自动化测试证明样例行为，不替代规范和不变量证明。

## 附录：模板

### Finding 模板

```markdown
## <分片>-<序号>：<结论>

- 分类：缺陷 | 未证明不变量 | 设计缺口 | 文档漂移 | 观察项
- 严重度：P0 | P1 | P2 | P3
- 置信度：已确认 | 高 | 中 | 低
- 状态：待核验 | 已核验 | 待决策 | 待修复 | 已收口

### 契约

- 外部规范：`references/...`「<小节>」
- 项目设计：`notes/...`「<小节>」
- 要求：<规范或设计实际要求的内容>

### 实现

- `path:line`：<实现行为>
- 调用链或状态转换：<从入口到故障点>

### 影响

<可达条件、错误结果、受影响不变量；不能证明可达时明确写出缺失证据。>

### 处理方向

<最小正确性修复；涉及架构选择时列出选项与独立理由，不直接选定。>

### 核验

- 规范解释：通过 | 未通过 | 待核验
- 路径可达性：通过 | 未通过 | 待核验
- 相邻调用点：已覆盖 | 有遗漏 | 待核验
- 核验者：<agent/human>
```

严重度定义：

- **P0**：可破坏内存安全、隔离或内核核心不变量；
- **P1**：可导致错误结果、用户可触发的内核失败、丢失唤醒或永久停滞；
- **P2**：当前路径尚可运行，但关键不变量无法证明或设计缺口会阻塞后续机制；
- **P3**：文档漂移、诊断质量或局部维护性问题。

### 分片模板

```markdown
# <分片名称>

- 基线：`<commit>`
- 状态：准备 | 首审 | 独立核验 | 待决策 | 待修复 | 已收口
- 外部规范：<固定版本与文件>
- 项目设计：<notes 文件>

## 代码表面

- 入口：
- 封装：
- 调用者：
- 共享状态与布局：
- 失败与退出路径：

## 契约表

| ID | 契约 | 来源 | 实现点 | 结论 |
|---|---|---|---|---|

## Findings

按附录 finding 模板填写。

## 跨片登记

只登记目标分片和问题摘要，不在本片展开。

## 收口条件

- 每条契约均有结论；
- 首审 findings 已独立核验；
- 待决策项已单独提交用户；
- 修复后 diff review 能追溯到 finding；
- 可推出的持久设计已进入 `notes/`。
```
