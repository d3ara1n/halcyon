# Agent 规范与接力导航整理

## 范围与基线

- 用户已确认规范整理建议；组件身份规则继续全仓生效，内核最终构建身份为 `kernel`，当前 `erhino_kernel` 属于待修正现状，本轮只登记后续迁移。
- 基线：`task/fal-service-capabilities` / `2811d48`。已有未提交 FAL 代码、Justfile、设计/实现文档与计划改动，全部保留。本轮仅编辑规范与导航相关文档。
- 目标：消除规则冲突，明确流程触发与唯一完成条件，区分交付前结构检查和提交后独立 Review，减少常驻重复规则与历史指令干扰。
- 非目标：内核改名实施、FAL 施工、代码行为改变、全仓历史文档改写、项目外全局规则写入、提交或 push。

## 工作安排

1. 核对现有规则、实际构建入口和接力位置，保存整理前 COMPASS 快照。
2. 整理 AGENTS、Review 纪律、文档入口和内核边界表述；将构建/平台细节集中到按需手册，COMPASS 只保留当前导航与证据索引。
3. 登记内核统一命名的唯一后续计划；验证本轮链接、规则一致性、历史证据及原有工作树改动的保留。

## 验证门

- 本轮只改 Markdown，采用差异检查、相对链接及必要锚点核验、旧冲突表述检索和实际配置对照，不运行 Rust/QEMU 验收。
- 整理前导航以只读快照保留；现有 FAL 唯一计划、阻断项和下一步入口不丢失。
- 不将未来 `kernel` 名称写成已完成的构建事实，不让文档示例提前使用不存在的二进制。

## 完成记录

2026-09-22 文档整理完成，未提交。

- AGENTS 将规则集中为心智模型、按需入口、操作约定、适用范围、唯一完成条件、施工流程和债务登记；明确当前会话决定立即生效及已授权范围内自主实施。工程限额按证据重评，不自动放宽。
- REVIEW 区分交付前结构检查和固定提交独立审查，明确活跃报告可维护 findings 处置，审查计划向报告移交唯一工作入口。
- 构建与平台细节迁入 `plans/BUILD-AND-TEST.md`，按当前配置修正浮动 nightly、内核/用户态 JSON target、check 作用域和 acceptance 路线；README 与工具手册同步入口。
- kernel idea 明确每次推进有界与内核提交后责任，不把内核退休转成用户维护；用户态 README 明确身份纪律全仓生效，用户态角色前缀范围保持清楚。
- COMPASS 去除历史施工叙述与重复戒律，保留 FAL 主线断点、全部原活跃 todo 链接、待触发事项和证据索引；完整原文保存在 [整理前快照](ref-2026-09-22-compass-snapshot.md)。
- `kernel` 改名由 [唯一后续计划](../todo-2026-09-22-kernel-identity.md)承接，未改 Cargo、Justfile、工具脚本或代码；原有 FAL 未提交改动保留。

## 验证证据与边界

- 相对链接检查通过：12 份文档（含 notes 索引与原导航快照），归档前 213 个本地链接、9 个标题锚点；归档后再次核验最终 215 个链接与 9 个锚点，工作记录与导航迁移有效。检查中修正了公共操作交付档案的日期链接。
- 实际配置对照通过：两个 workspace 的 JSON target 路径存在且与手册一致；`rust-toolchain` 当前为 `nightly`；内核 package 与 Justfile 仍使用 `erhino_kernel`。
- 快照验证通过：反向还原机械链接调整后保留原 121 行，与 HEAD 的差异恰为原先三处未提交 FAL 导航更新（原第 29、37、49 行）；原活跃 todo 链接和当前接力标记在新导航中完整保留。
- `git diff --check` 通过；已核对本轮差异、规则迁移及旧冲突表述。AGENTS 由约 22 KB 缩至约 15 KB，COMPASS 由约 36 KB 缩至约 9 KB；历史内容保留在只读快照。
- 未运行 Rust/QEMU：本轮没有执行行为改动。未执行提交后独立 Review：本轮尚未提交，不能预造固定提交证据。

本轮无未闭合文档整改项。内核改名是独立后续工作；FAL 继续由原计划拥有，其开发证据未升级为完成结论。
