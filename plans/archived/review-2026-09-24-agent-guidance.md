# Agent 规范与导航固定提交审查（2026-09-24）

> 当前状态：P2 finding 已在当前工作树的后续文档同步中修复并复核，报告已关闭并归档。审查对象仍为固定提交 `60e3253`（`docs: 整理代理规则、完成条件与接力导航`）相对父提交 `2811d48`；不将后续 FAL/内核实现改动倒算为该固定提交的证据。

## 范围与证据

本次只读核对固定提交实际变更及其文档契约：`AGENTS.md`、`plans/REVIEW.md`、`plans/BUILD-AND-TEST.md`、`plans/COMPASS.md`、整理档案和历史快照。检查内容包括规则冲突、完成条件、授权和施工/Review/归档流程、导航与唯一工作记录的可接手性、有效命令和链接、历史快照边界。

审查使用 `git show 60e3253`、父提交对照、固定提交内的相对链接和记录内容；未运行 Rust/QEMU，也未把当前未提交工作树计入结论。

## Findings

### P2：固定提交的当前导航无法由其唯一计划独立承接

证据：

- `60e3253:plans/COMPASS.md:7-11` 将当前接手写成 D1 已删除、D2/D3 已有局部实现，并指向 FAL 计划的接力断点。
- 同一提交的 `plans/todo-2026-09-fal-service-capabilities.md:3,415-425` 仍记载“下一步为 F3d 实施”，并写明没有新增运行代码或运行验证。
- `plans/archived/ref-2026-09-22-compass-snapshot.md:3,36` 只保留未提交工作树摘要，并明确它是历史追溯快照，不能替代当前唯一工作记录。

从 `60e3253` 单独接手时，当前导航与唯一 FAL 计划会给出相反的施工状态；读者无法判断 D1/D2/D3 的真实进度、阻断项和下一步，也无法仅依赖该提交恢复施工责任链。

建议：后续文档整理提交应让当前导航只陈述固定提交可核实的状态，或在未提交工作树尚未进入唯一计划时，明确将当前接手标为“依赖外部未提交工作树、不可独立接手”，不得同时使用进行中阶段命令作为现行施工指令。修复后重新核对 Compass、唯一计划与历史快照三者边界，再关闭本 finding。

当前状态：已关闭。历史快照保留原事实；修复只更新当前有效的 Compass、FAL 唯一计划和接手边界。

## 无 finding 的检查项

- 未发现 AGENTS/REVIEW 中关于授权、完成条件、交付前检查和独立 Review 生命周期的规则冲突。
- 未发现 BUILD-AND-TEST 中与固定提交 Cargo 配置、Justfile 路线或 target 事实矛盾的有效命令。
- 固定提交的相对文档链接存在且可解析。
- 快照正文与父提交 COMPASS 的差异符合提交声明；历史“下一步”已明确标为历史而非现行指令。
- 内核旧名、JSON target、浮动 nightly 和验收路线在固定提交自身范围内叙述一致。

## 复核门

修复后检查：

1. 固定提交可独立解释当前导航，或明确记录不可独立接手的外部前提。
2. 唯一工作计划与 Compass 对同一任务的状态、阻断项和下一步一致。
3. 历史快照保留原事实，不被批量改写。
4. 重新执行文档链接、锚点和差异核对；本审查仍不替代 FAL 或内核实现验收。

本次未执行 commit、push 或外部写入。

## 修复与复核

当前工作树已完成后续文档同步：`plans/COMPASS.md` 将 FAL 标为已完成并将当前接手切换到固定提交审查；`plans/todo-2026-09-fal-service-capabilities.md` 顶部、F3f/F4 阶段表和验证边界已同步为最终状态，能够独立说明当前交付、保留限制和后续入口。历史快照未修改。

复核结果：

- Compass 当前接手、FAL 唯一计划状态和固定 Review 入口一致。
- FAL 计划不再把 F3d/F4-3 描述为未来施工，保留限制均有 owner 与触发条件。
- 历史快照继续作为历史事实，不承担当前施工指令。
- `git diff --check` 通过；当前代码与内核身份改名的验证证据不作为本 Review 的额外结论。

Finding 已关闭；本报告随后归档。
